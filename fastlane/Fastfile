desc "Run the tests and prepare for Danger"
lane :test do |options|

  scan(
    derived_data_path: "temp/derived/",
    xcodebuild_formatter: "xcpretty -f `xcpretty-json-formatter`",
    code_coverage: true,
    build_for_testing: true
  )
end
